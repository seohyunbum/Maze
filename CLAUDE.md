# 손으로 그리는 미로

아이와 함께 만드는 미로 게임. 의존성 없는 단일 HTML 파일(`index.html`)이며,
파일을 브라우저로 바로 열면 실행된다. main 에 푸시하면 GitHub Pages 가 자동으로 다시 배포한다.

## 미로 규칙 (앞으로 만드는 **모든** 미로에 적용)

1. **손이 지나간 자리는 검은색으로 그려진다.**
   손가락(터치)이나 마우스를 끌면 지나간 궤적이 검은 선으로 남는다.
2. **검은색 벽은 절대 넘어갈 수 없다.**
   벽에 부딪히면 거기서 멈춘다. 빠르게 긋거나 마구 휘저어도 통과할 수 없다.

새 미로를 추가하거나 기존 미로를 고칠 때 이 두 규칙을 반드시 지킬 것.

### 구현 메모 (`index.html`)

- 미로는 `(2n+1) × (2n+1)` 격자로 표현한다. `1` = 검은 벽, `0` = 흰 통로.
- 생성은 recursive backtracker — 항상 정답이 존재하는 미로가 나온다.
- **한 번 만들고 끝내지 않는다.** 같은 크기라도 뽑기 운에 따라 정답이 짧고
  갈림길이 없는 미로가 나온다(실측: 22×22 에서 갈림길 3개짜리까지 나왔다).
  `genBestMaze()` 가 후보 40개를 만들어 `rateMaze()` 점수가 가장 높은 것만 쓴다.
  점수 = 정답길이 + 6×갈림길 + 2×미끼깊이. 정답이 `3*(가로+세로)` 보다 짧은
  후보는 아예 제외한다. 250ms 를 넘기면 중단해 느린 기기를 보호한다.
- 좌표 단위(unit)는 "격자 한 칸 = 1"로 두고 픽셀과 분리한다.
  덕분에 창 크기가 바뀌어도 그려둔 선과 펜 위치가 그대로 유지된다.
- 벽을 못 넘게 하는 핵심 세 가지:
  - `STEP`(0.07 unit)만큼 잘게 쪼개 이동한다 → 한 번에 벽을 건너뛰지 못한다.
  - `free()`는 점이 아니라 펜 굵기(`PEN_R`)의 둘레 9군데를 함께 검사한다
    → 선이 벽을 파고들지 않는다.
  - `pointermove`에서 `getCoalescedEvents()`로 중간 좌표까지 전부 처리한다
    → 빠르게 그어도 좌표가 건너뛰지 않는다.
  - 막히면 x축·y축을 따로 시도해 벽을 따라 미끄러지게 한다 (모서리 돌기).
- 펜은 현재 펜 끝에서 `GRAB_R` 안쪽을 눌러야 이어 그릴 수 있다.
  아무 데나 눌러 순간이동하는 것을 막기 위함.

### 난이도 실측 (후보 선별 전 → 후, 각 60회 평균)

| 크기  | 정답길이 평균(최소) | 갈림길 평균(최소) | 미끼깊이 | 생성 |
|-------|---------------------|-------------------|----------|------|
| 10×10 |  99(47) → 135(115)  |   5(2) → 10(7)    |  38 → 58 |  2ms |
| 16×16 | 211(87) → 338(287)  |  10(3) → 18(13)   | 71 → 115 |  4ms |
| 22×22 | 341(143) → 593(499) |  14(4) → 30(23)   | 96 → 196 |  8ms |
| 28×28 | 536(239) → 906(735) |  21(6) → 43(32)   |142 → 273 | 12ms |

난이도를 더 손볼 때는 이 표를 다시 재서 비교할 것. 평균보다 **최소값**이 중요하다
— 쉬운 미로가 가끔 나오는 것이 체감 난이도를 가장 크게 떨어뜨린다.

### 휴대폰 앱 (PWA)

- `manifest.webmanifest` + `sw.js` + `icons/` 로 홈 화면에 설치된다. 빌드 도구 없음.
- `sw.js` 는 stale-while-revalidate — 오프라인에서 즉시 뜨고, 뒤에서 최신판을 받아둔다.
  **배포할 때마다 `CACHE` 버전을 올릴 것.** 안 올리면 설치한 사람에게 옛 화면이 남는다.
- `pickDims()` 가 화면 비율에 맞춰 미로 모양을 정한다. 정사각 미로를 세로 화면에 넣으면
  위아래가 남고 칸이 작아진다. 짧은 쪽을 난이도로 고정하고 긴 쪽을 늘린다(최대 3.6배).
- 손가락 화면에서는 칸이 `MIN_CELL`(10px)보다 작아지지 않게 난이도를 자동으로 낮춘다.
  가로로 눕힌 휴대폰이 특히 그렇다 — 높이가 얼마 안 남아 칸이 6px 까지 떨어졌었다.
- `pickDims()` 와 `layout()` 은 반드시 `stageBox()` 로 같은 영역을 봐야 한다.
  한쪽만 패딩을 빼면 예측한 칸 크기와 실제가 어긋나 MIN_CELL 보장이 깨진다.
- `grabOff`: 잡은 순간의 손가락↔펜 간격을 유지한다. 펜이 손가락으로 순간이동하지 않고,
  펜 살짝 아래를 잡으면 손에 가려지지 않는다.
- `getCoalescedEvents()` 가 **빈 배열**을 주는 경우가 있다. 그대로 쓰면 입력이 통째로
  무시돼 펜이 안 움직인다. 비어 있으면 이벤트 자체를 쓸 것.

### 검증

Playwright로 다음을 확인했다: 벽 무시 직선 긋기·무작위 휘젓기에도 선이
벽을 침범하지 않고 골인되지 않을 것, 정답 경로대로 가면 통과할 것,
난이도 4종 모두 정답이 존재할 것, 리사이즈 후에도 그림이 유지될 것.
