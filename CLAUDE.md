# 손으로 그리는 미로

아이와 함께 만드는 미로 게임. 의존성 없는 단일 HTML 파일(`index.html`)이며,
파일을 브라우저로 바로 열면 실행된다. main 에 푸시하면 GitHub Pages 가 자동으로 다시 배포한다.

## 미로 규칙 (앞으로 만드는 **모든** 미로에 적용)

1. **손이 지나간 자리는 검은색으로 그려진다.**
   손가락(터치)이나 마우스를 끌면 지나간 궤적이 검은 선으로 남는다.
2. **검은색 벽은 절대 넘어갈 수 없다.**
   벽에 부딪히면 거기서 멈춘다. 빠르게 긋거나 마구 휘저어도 통과할 수 없다.

새 미로를 추가하거나 기존 미로를 고칠 때 이 두 규칙을 반드시 지킬 것.

### 구현 메모 (`index.html`)

- 미로는 `(2n+1) × (2n+1)` 격자로 표현한다. `1` = 검은 벽, `0` = 흰 통로.
- 생성은 recursive backtracker — 항상 정답이 존재하는 미로가 나온다.
- **한 번 만들고 끝내지 않는다.** 후보를 여러 개 만들어 가장 어려운 것만 쓴다.
- **난이도를 추측하지 말고 재라.** `walkCost()` 가 사람처럼 푸는 흉내를 낸다
  (목표 쪽으로 가고, 막히면 되돌아 나오며, 되돌아 나오는 칸도 비용으로 센다).
  `rateMaze()` 점수 = 헤맨 칸 수. 잘 푸는 사람도 헤매야 진짜 어려우므로
  greedy 쪽에 1.4배 가중.
- **길이는 난이도가 아니다.** 이전 버전은 점수에 정답 길이를 넣었다가
  "길기만 하고 쉬운" 미로를 만들었다. 실측하니 길이로 40개 중 최고를 뽑는 것이
  오히려 미로를 **더 쉽게** 만들었다(순수 DFS 1.98배 → 길이선별 1.17배).
  정답이 미로 전체를 훑고 지나가면 헤맬 곁가지가 남지 않기 때문이다.
- **river 를 낮추는 것이 난이도에 가장 크게 기여한다.** growing tree 에서
  river=1 은 재귀 백트래커(긴 복도·갈림길 적음), 낮추면 가지가 무성해진다.
  난이도별 `RIVER` 표: 10→0.8, 16→0.5, 22→0.35, 28→0.3, 극악 30→0.22.
- **브레이딩(고리 만들기)은 역효과였다.** 막다른 길을 허물어 고리를 만들면
  한손벽짚기는 깨지지만, 고리가 지름길이 되어 잘 푸는 사람에게는 오히려 쉬워진다
  (river0.3 에서 헤맨배수 6.4배 → 브레이딩 80% 적용시 1.6배). 쓰지 않는다.
- 좌표 단위(unit)는 "격자 한 칸 = 1"로 두고 픽셀과 분리한다.
  덕분에 창 크기가 바뀌어도 그려둔 선과 펜 위치가 그대로 유지된다.
- 벽을 못 넘게 하는 핵심 세 가지:
  - `STEP`(0.07 unit)만큼 잘게 쪼개 이동한다 → 한 번에 벽을 건너뛰지 못한다.
  - `free()`는 점이 아니라 펜 굵기(`PEN_R`)의 둘레 9군데를 함께 검사한다
    → 선이 벽을 파고들지 않는다.
  - `pointermove`에서 `getCoalescedEvents()`로 중간 좌표까지 전부 처리한다
    → 빠르게 그어도 좌표가 건너뛰지 않는다.
  - 막히면 x축·y축을 따로 시도해 벽을 따라 미끄러지게 한다 (모서리 돌기).
- 펜은 현재 펜 끝에서 `GRAB_R` 안쪽을 눌러야 이어 그릴 수 있다.
  아무 데나 눌러 순간이동하는 것을 막기 위함.

### 난이도 실측 (길이기준 선별 → 헤맴기준 선별, 각 25회 평균)

핵심 지표는 **헤맨 배수** = 잘 푸는 사람이 실제로 손을 움직인 칸 ÷ 정답 길이.
1에 가까우면 그냥 복도를 걸어간 것이고, 클수록 헤맨 것이다.

| 크기  | river | 헤맨 배수      | 정답 길이   | 생성 |
|-------|-------|----------------|-------------|------|
| 10×10 | 0.80  | 1.11x → 5.6x   | 152 →  54   |  2ms |
| 16×16 | 0.50  | 1.19x → 9.4x   | 347 →  87   |  5ms |
| 22×22 | 0.35  | 1.23x → 13.0x  | 603 → 116   |  9ms |
| 28×28 | 0.30  | 1.26x → 17.0x  | 908 → 145   | 15ms |
| 30×30 | 0.22  | 1.29x → 19.6x  |1029 → 145   | 37ms |

정답 길이가 **줄었는데** 어려워진 것이 핵심이다 — 지루함이 아니라 난이도를 늘렸다.
난이도를 더 손볼 때는 이 표를 다시 재서 비교할 것. 길이·갈림길 같은 대리 지표를
믿지 말고 `walkCost()` 로 직접 재라.

### 휴대폰 앱 (PWA)

- `manifest.json` + `sw.js` + `icons/` 로 홈 화면에 설치된다. 빌드 도구 없음.
- `sw.js` 는 stale-while-revalidate — 오프라인에서 즉시 뜨고, 뒤에서 최신판을 받아둔다.
  **배포할 때마다 `CACHE` 버전을 올릴 것.** 안 올리면 설치한 사람에게 옛 화면이 남는다.
- `pickDims()` 가 화면 비율에 맞춰 미로 모양을 정한다. 정사각 미로를 세로 화면에 넣으면
  위아래가 남고 칸이 작아진다. 짧은 쪽을 난이도로 고정하고 긴 쪽을 늘린다(최대 3.6배).
- 손가락 화면에서는 칸이 `MIN_CELL`(10px)보다 작아지지 않게 난이도를 자동으로 낮춘다.
  가로로 눕힌 휴대폰이 특히 그렇다 — 높이가 얼마 안 남아 칸이 6px 까지 떨어졌었다.
- `pickDims()` 와 `layout()` 은 반드시 `stageBox()` 로 같은 영역을 봐야 한다.
  한쪽만 패딩을 빼면 예측한 칸 크기와 실제가 어긋나 MIN_CELL 보장이 깨진다.
- `grabOff`: 잡은 순간의 손가락↔펜 간격을 유지한다. 펜이 손가락으로 순간이동하지 않고,
  펜 살짝 아래를 잡으면 손에 가려지지 않는다.
- 설치 안내 시트(`#install`)는 열자마자 뜬다. 안드로이드는 `beforeinstallprompt` 를
  잡아 '설치하기' 한 번으로 끝내고, 아이폰은 API 가 없어 방법만 알려준다.
  아이폰 크롬에서는 설치 자체가 불가능하므로 사파리로 열라고 안내한다.
  `prompt()` 는 사용자가 누른 직후에만 열린다 — 자동 호출은 브라우저가 막는다.
- **안드로이드에서 `beforeinstallprompt` 는 안 올 수도 있다.** 브라우저 종류, 이미 설치함,
  조건 미충족 등. 그때 아무것도 안 띄우면 사용자는 "왜 안 되지" 로 끝난다.
  3.5초 기다렸다가 안 오면 `android-manual` 시트로 브라우저 메뉴 경로를 알려준다
  (삼성 인터넷: ≡ → 현재 페이지 추가 → 홈 화면 / 크롬: ⋮ → 앱 설치).
  갤럭시 기본 브라우저는 크롬이 아니라 삼성 인터넷인 경우가 많다.
- **설치 실패의 1순위 원인은 '앱 안의 브라우저'다.** 카카오톡·네이버앱·인스타에서 열면
  `beforeinstallprompt` 가 아예 오지 않고 아이폰도 '홈 화면에 추가'가 없다. UA 로 감지해
  `kakaotalk://web/openExternal` 또는 안드로이드 `intent://` 로 밖으로 내보낸다.
- 인트로(SEO 로고)는 z-index 최상단으로 화면을 4.4초 덮는다. 그 밑에 무언가를 띄우면
  보이지도 눌리지도 않는다. `whenIdentGone()` 으로 사라진 뒤에 띄울 것.
- 매니페스트는 `.webmanifest` 대신 **`manifest.json`** 을 쓴다. 정적 호스팅에서
  `.webmanifest` 가 엉뚱한 MIME 로 나가면 브라우저가 무시해 설치가 조용히 실패한다.
- **`display` 를 지정한 요소에는 `[hidden]{display:none}` 을 같이 써야 한다.**
  안 그러면 `hidden` 속성이 CSS 에 져서 시트가 닫히지 않고, 이미 설치한 사람에게도 뜬다.
- `getCoalescedEvents()` 가 **빈 배열**을 주는 경우가 있다. 그대로 쓰면 입력이 통째로
  무시돼 펜이 안 움직인다. 비어 있으면 이벤트 자체를 쓸 것.

### 검증

Playwright로 다음을 확인했다: 벽 무시 직선 긋기·무작위 휘젓기에도 선이
벽을 침범하지 않고 골인되지 않을 것, 정답 경로대로 가면 통과할 것,
난이도 4종 모두 정답이 존재할 것, 리사이즈 후에도 그림이 유지될 것.
