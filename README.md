# 🌀 손으로 그리는 미로

### ▶️ 지금 바로 하기 — https://seohyunbum.github.io/maze/

링크만 있으면 친구도 바로 할 수 있습니다. 설치도, 로그인도 필요 없습니다.
휴대폰·태블릿에서도 손가락으로 됩니다.
(`index.html` 파일 하나만 내려받아 브라우저로 열어도 똑같이 실행됩니다.)

## 규칙

- 초록색 **시작**(왼쪽 위)에서 손가락이나 마우스로 끌어 빨간색 **도착**(오른쪽 아래)까지 가면 통과!
- 손이 지나간 자리는 **검은색 선**으로 그려집니다.
- **검은색 벽은 넘어갈 수 없습니다.** 벽에 닿으면 거기서 멈춥니다.

## 기능

- 난이도 4단계 (8×8 / 12×12 / 16×16 / 22×22) — 고를 때마다 새 미로 생성
- `새 미로` 새로 만들기 · `다시 그리기` 선만 지우기 · `정답 보기` 힌트 경로 표시
- 걸린 시간과 난이도별 최고기록 저장, 벽에 막힌 횟수 표시
- 화면 크기에 맞춰 자동 조정, 밝은/어두운 화면 모두 대응

미로는 매번 새로 만들어지며 **항상 풀 수 있는 미로**만 생성됩니다.

## 배포

`main` 에 푸시하면 GitHub Actions 가 GitHub Pages 로 자동 배포한다
(`.github/workflows/pages.yml`). 의존성이 없는 단일 HTML 파일이라 빌드 단계는 없다.
